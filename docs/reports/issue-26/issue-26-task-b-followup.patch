diff --git a/packages/workshop-frontend/src/MarketingLandingPage.test.tsx b/packages/workshop-frontend/src/MarketingLandingPage.test.tsx
index 8c753c3..e181a50 100644
--- a/packages/workshop-frontend/src/MarketingLandingPage.test.tsx
+++ b/packages/workshop-frontend/src/MarketingLandingPage.test.tsx
@@ -234,7 +234,7 @@ describe('Marketing Landing Page', () => {
       vi.stubGlobal('fetch', fetchMock)
       Object.defineProperty(Element.prototype, 'scrollIntoView', {
         configurable: true,
-        value: vi.fn(),
+        value: vi.fn<Element['scrollIntoView']>(),
       })
       await renderPage({ href })
 
@@ -349,7 +349,7 @@ describe('Marketing Landing Page', () => {
     vi.stubGlobal('fetch', vi.fn<typeof fetch>().mockResolvedValue(successResponse(angles)))
     Object.defineProperty(Element.prototype, 'scrollIntoView', {
       configurable: true,
-      value: vi.fn(),
+      value: vi.fn<Element['scrollIntoView']>(),
     })
     await renderPage({ href })
     await submitRun(product, market)
diff --git a/packages/workshop-frontend/src/root-auth.test.tsx b/packages/workshop-frontend/src/root-auth.test.tsx
index d752235..fe0b1c4 100644
--- a/packages/workshop-frontend/src/root-auth.test.tsx
+++ b/packages/workshop-frontend/src/root-auth.test.tsx
@@ -104,8 +104,8 @@ describe('root session validation', () => {
   }
 
   it.each([
-    { href: '/', heading: 'From brief to usable result.' },
-    { href: '/zh', heading: '从任务到可用成果。' },
+    { href: '/', heading: 'AI UGC ads start with the angle, not the prompt' },
+    { href: '/zh', heading: 'AI UGC 广告的起点是角度，不是提示词' },
   ])('shows the localized Marketing Landing Page with no stored token at $href', async ({ href, heading }) => {
     window.history.replaceState({}, '', href)
     const api = createPublicApi(() => { throw new Error('unexpected authentication') })
@@ -151,8 +151,8 @@ describe('root session validation', () => {
   })
 
   it.each([
-    { href: '/', heading: 'From brief to usable result.' },
-    { href: '/zh', heading: '从任务到可用成果。' },
+    { href: '/', heading: 'AI UGC ads start with the angle, not the prompt' },
+    { href: '/zh', heading: 'AI UGC 广告的起点是角度，不是提示词' },
   ])('clears an invalid session and shows the Marketing Landing Page at $href', async ({ href, heading }) => {
     window.history.replaceState({}, '', href)
     localStorage.setItem('authToken', 'invalid-token')
